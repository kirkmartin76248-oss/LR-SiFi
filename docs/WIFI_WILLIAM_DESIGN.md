# WiFi William — Legacy Design Reference

This document contains earlier hardware and firmware design decisions and historical notes.

**Current system architecture source of truth:** `docs/WIFI_WILLIAM_SYSTEM_ARCHITECTURE.md`

The current architecture supersedes this document where they conflict. In particular, the current system uses **ESP-NOW from Node to Hub**, the product is under **Krek Labs, LLC**, and the mobile-app/customer multi-user + Google Sheet architecture is defined in the current system architecture document.

Hardware details in this file remain useful as historical/reference material until the current hardware/firmware specification is updated to match the latest architecture.
